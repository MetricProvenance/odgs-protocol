{
  "name": "odgs",
  "version": "2.0.1",
  "description": "ODGS Protocol Library (Node.js)",
  "main": "lib/index.js",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "prepublishOnly": "python3 ./scripts/sync_schemas.py"
  },
  "files": [
    "lib"
  ],
  "dependencies": {
    "ajv": "^8.12.0"
  },
  "devDependencies": {
    "vitest": "^1.0.0"
  }
}
