# ─────────────────────────────────────────────────────
# ODGS Protocol — Environment Configuration
# ─────────────────────────────────────────────────────
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# .env is gitignored and should NEVER be committed.
# ─────────────────────────────────────────────────────

# ── AI Factory (Sovereign Jurist) ──
# Required for: odgs generate, AI Factory tab, /api/agent/* endpoints
# Get a key at: https://ai.google.dev/
GEMINI_API_KEY=

# ── AI Model Selection ──
# Default: gemini-3.0-flash-preview
# Options: gemini-2.5-pro-preview-05-06, gemini-2.0-flash, etc.
GEMINI_MODEL_NAME=gemini-3.0-flash-preview

# ── Project Root Override (optional) ──
# Only needed if running from a non-standard directory structure.
# ODGS_PROJECT_ROOT=/path/to/odgs-protocol-main
